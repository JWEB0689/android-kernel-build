version: '3.8'

services:
  kernel-builder:
    build: .
    container_name: lineage-sm8550-builder
    volumes:
      - ./build-output:/build/out
    environment:
      - KERNEL_VERSION=lineage-23.2-sm8550
    stdin_open: true
    tty: true
